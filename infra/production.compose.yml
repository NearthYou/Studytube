name: studytube

services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: studytube-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: "${POSTGRES_USER:?POSTGRES_USER is required}"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}"
      POSTGRES_DB: "${POSTGRES_DB:?POSTGRES_DB is required}"
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s

  valkey:
    image: valkey/valkey:9.1.1-alpine
    container_name: studytube-valkey
    restart: unless-stopped
    command: ["valkey-server", "--appendonly", "yes", "--appendfsync", "everysec"]
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - valkeydata:/data
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 5s

  caddy:
    image: caddy:2.11.4-alpine
    container_name: studytube-caddy
    restart: unless-stopped
    network_mode: host
    read_only: true
    environment:
      STUDYTUBE_SITE_ADDRESS: "${STUDYTUBE_SITE_ADDRESS:?STUDYTUBE_SITE_ADDRESS is required}"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - /var/www/studytube:/var/www/studytube:ro
      - caddy_data:/data
      - caddy_config:/config
    tmpfs:
      - /tmp

volumes:
  pgdata:
  valkeydata:
  caddy_data:
  caddy_config:
