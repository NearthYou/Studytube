## 오늘 할 일

- AI Agent 기능 개념 정리

- API 명세서 작성

API list

Auth API
- 회원가입
- 로그인
- 로그아웃
- 이메일 인증번호 발송
- 이메일 인증번호 확인
- 이메일 중복 확인
- 닉네임 중복 확인
- 소셜 로그인 요청
- 소셜 로그인 콜백

Post API
- 게시글 목록 조회
- 게시글 상세 조회
- 게시글 작성
- 게시글 수정
- 게시글 삭제
- 게시글 검색
- 게시글 조회수 증가
- 게시글 이미지 업로드
- 게시글 이미지 삭제

Category API
- 카테고리 목록 조회
- 카테고리별 게시글 조회

Comment API
- 댓글 목록 조회
- 댓글 작성
- 댓글 수정
- 댓글 삭제

Like API
- 게시글 좋아요
- 게시글 좋아요 취소
- 댓글 좋아요
- 댓글 좋아요 취소

Pet Place API
- 반려동물 동반 장소 지역 기반 조회
- 반려동물 동반 장소 위치 기반 조회
- 반려동물 동반 장소 키워드 검색
- 반려동물 동반 장소 상세 조회

AI API
- AI Agent 질의

Internal Tool
- 사용자 의도 분석
- RAG Tool
- MCP TourAPI Tool
- 일반 대화 생성
- 최종 LLM 응답 생성
- Safety Guardrail 검사

- nest.js

이메일 인증

Controller: 요청을 받고 응답을 돌려주는 역할

Provider/Service: 복잡한 비즈니스 로직을 맡는 역할

Module: 관련 기능을 묶는 단위

DTO검증: ValidationPipe + class-validator

#### 1단계

1. DTO 검증용 패키지 설치

npm i class-validator class-transformer

2. Nest 앱 전체에 validationPipe적용

import { ValidationPipe } from '@nestjs/common';

app.useGlobalPipes(
    new ValidationPipe({
      whitelist: true,
      transform: true,
    }),
  );

#### 2단계

1. src/auth 폴더 생성
2. DTO 생성
3. Controller생성
4. Service 생성
5. AuthModule을 AppModule에 연결

mkdir -p src/auth/dto

touch src/auth/auth.module.ts
touch src/auth/auth.controller.ts
touch src/auth/auth.service.ts
touch src/auth/dto/request-email-verification.dto.ts

DTO 작성

import { IsEmail } from 'class-validator';

export class RequestEmailVerificationDto {
  @IsEmail()
  email: string;
}

import { Injectable } from '@nestjs/common';
import { RequestEmailVerificationDto } from './dto/request-email-verification.dto';

@Injectable()
export class AuthService {
  requestEmailVerification(dto: RequestEmailVerificationDto) {
    return {
      message: '인증 이메일 요청을 접수했습니다.',
      email: dto.email,
    };
  }
}

import { Body, Controller, Post } from '@nestjs/common';
import { AuthService } from './auth.service';
import { RequestEmailVerificationDto } from './dto/request-email-verification.dto';

@Controller('auth')
export class AuthController {
  constructor(private readonly authService: AuthService) {}

  @Post('email-verification/request')
  requestEmailVerification(@Body() dto: RequestEmailVerificationDto) {
    return this.authService.requestEmailVerification(dto);
  }
}

import { Module } from '@nestjs/common';
import { AuthController } from './auth.controller';
import { AuthService } from './auth.service';

@Module({
  controllers: [AuthController],
  providers: [AuthService],
})
export class AuthModule {}



### Nest

- 데코레이터란?
클래스, 메서드, 파라미터, 프로퍼티 에 "이 코드가 어떤 역할인지" 표시하는 문법

- 의존성 주입이란? Dependency injecton
필요한 객체를 내가 만들지 않고 받아서 쓰는 것

